# Python >= 3.10
# 一键入口：真实依赖声明在 pyproject.toml（corenova-verify + extras），此处不重复列举。
#   core  = PyYAML + boto3（验证/发布链路运行时）
#   apps  = requests + playwright（应用验证：测试模板与截图）
#   dev   = pytest + pytest-timeout（本仓测试套件）
#
# 装完即包含 corenova 包本身（editable），scripts/ 入口脚本因此不需要 sys.path hack。
-e .[dev,apps]
